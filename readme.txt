
Installing from Conda/Binstar

First you must have Conda (http://continuum.io/downloads) installed, then open a terminal or a command prompt window and run:

conda install -c https://conda.binstar.org/decarlof dxfile
